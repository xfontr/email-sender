export const objectKeys = (item: object) => Object.keys(item);

export const objectValues = (item: object) => Object.values(item);

export const objectEntries = (item: object) => Object.entries(item);
