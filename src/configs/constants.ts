/** Flags that will be ignored */
// eslint-disable-next-line import/prefer-default-export
export const META_FLAGS = ["contactData"];

export const FLAG_FORMAT = (content: string) => `{{ ${content} }}`;
